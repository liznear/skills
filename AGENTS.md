# Agents

## Auto-Learnings
- When saving research files, always use the directory structure `公司名/日期.md`. Do not use numbered prefixes (like `00_`) or descriptive non-company-name directories (like `初筛与配置建议`) for summary/overview files. If an overview file is needed, ask the user where they'd like it placed or place it under a company-name-consistent directory. Always confirm the desired file organization before writing multiple files.
